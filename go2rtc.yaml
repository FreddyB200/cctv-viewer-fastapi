streams:
  cam1: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=1&subtype=1
  cam2: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=2&subtype=1
  cam3: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=3&subtype=1
  cam4: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=4&subtype=1
  cam5: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=5&subtype=1
  cam6: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=6&subtype=1
  cam7: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=7&subtype=1
  cam8: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=8&subtype=1
  cam9: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=9&subtype=1
  cam10: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=10&subtype=1
  cam11: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=11&subtype=1
  cam12: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=12&subtype=1
  cam13: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=13&subtype=1
  cam14: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=14&subtype=1
  cam15: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=15&subtype=1
  cam16: rtsp://${CAM_USER}:${CAM_PASS}@${CAM_IP}:${CAM_PORT}/cam/realmonitor?channel=16&subtype=1

api:
  listen: ":1984"
  origin: "*"  # Allow all origins (for development)

webrtc:
  listen: ":8555"
  ice_servers:
    - urls: [stun:stun.l.google.com:19302]
    - urls: [turn:openrelay.metered.ca:80]
      username: openrelay
      credential: openrelay

log:
  level: info
