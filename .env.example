# =============================================================================
# Camera Configuration (Required)
# =============================================================================
# These variables are used by both FastAPI and go2rtc for RTSP camera connections
CAM_USER=admin
CAM_PASS=your_camera_password_here
CAM_IP=192.168.0.23
CAM_PORT=554
TOTAL_CAMERAS=16

# =============================================================================
# Security Configuration (Optional - Defaults for development)
# =============================================================================
# CRITICAL: In production, you MUST set ALLOWED_ORIGINS to your actual domain
# Examples:
#   Single domain: ALLOWED_ORIGINS=https://yourdomain.com
#   Multiple domains: ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
#   Development (default): ALLOWED_ORIGINS=*
ALLOWED_ORIGINS=*

# =============================================================================
# TURN Server Configuration (Optional - Public defaults for development)
# =============================================================================
# CRITICAL: In production, use your own private TURN server infrastructure
# Public TURN servers like openrelay can be abused and have bandwidth limits
#
# Example configurations:
#   coturn server: TURN_URL=turn:turn.yourdomain.com:3478
#   Twilio TURN: TURN_URL=turn:global.turn.twilio.com:3478?transport=udp
#
TURN_URL=turn:openrelay.metered.ca:80
TURN_USERNAME=openrelay
TURN_PASSWORD=openrelay

# =============================================================================
# Production Security Checklist
# =============================================================================
# Before deploying to production, ensure:
# [ ] ALLOWED_ORIGINS is set to your production domain(s)
# [ ] Private TURN server configured (coturn, Twilio, or cloud provider)
# [ ] Strong, unique passwords for CAM_PASS
# [ ] Firewall rules restrict access to ports 1984, 8555
# [ ] Consider implementing authentication for the WebRTC endpoint
# [ ] WebRTC connectivity tested from target network environments